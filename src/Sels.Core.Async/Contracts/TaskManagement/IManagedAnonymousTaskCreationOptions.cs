using Sels.Core.Async.TaskManagement;
using Sels.Core.Extensions;
using System;
using System.Collections.Generic;
using System.Text;
using System.Threading;
using System.Threading.Tasks;
using static Sels.Core.Delegates.Async;

namespace Sels.Core.Async.TaskManagement
{
    /// <summary>
    /// Exposes more options for <see cref="IManagedAnonymousTask"/>(s) scheduled using a <see cref="ITaskManager"/>.
    /// </summary>
    /// <typeparam name="TOutput">The output generated by the task</typeparam>
    public interface IManagedAnonymousTaskCreationOptions<TOutput> : IManagedTaskSharedCreationOptions<TOutput, IManagedAnonymousTaskCreationOptions<TOutput>>
    {
        /// <summary>
        /// Defines the factory for another managed task to run after the current one finishes executing regardless of the result.
        /// </summary>
        /// <param name="continuationFactory">Delegate that creates the managed task to run after the current one fails to execute. Null can be returned if no task needs to be scheduled</param>
        /// <returns>Current options for method chaining</returns>
        IManagedAnonymousTaskCreationOptions<TOutput> ContinueWith(Func<ITaskManager, IManagedAnonymousTask, object, CancellationToken, IManagedTask?> continuationFactory);
        /// <summary>
        /// Defines the factory for another managed task to run after the current one finishes executing regardless of the result.
        /// </summary>
        /// <param name="continuationFactory">Delegate that creates the managed task to run after the current one fails to execute. Null can be returned if no task needs to be scheduled</param>
        /// <returns>Current options for method chaining</returns>
        IManagedAnonymousTaskCreationOptions<TOutput> ContinueWith(Func<ITaskManager, IManagedAnonymousTask, object, CancellationToken, Task<IManagedTask>?> continuationFactory);
        /// <summary>
        /// Defines the factory for another managed task to run after the current one finishes executing regardless of the result.
        /// </summary>
        /// <param name="continuationFactory">Delegate that creates the managed task to run after the current one fails to execute. Null can be returned if no task needs to be scheduled</param>
        /// <returns>Current options for method chaining</returns>
        IManagedAnonymousTaskCreationOptions<TOutput> ContinueWith(Func<ITaskManager, IManagedAnonymousTask, object, CancellationToken, IManagedAnonymousTask?> continuationFactory);

        #region ContinueWith
        /// <summary>
        /// Defines the factory for another managed task to run after the current one successfully executes.
        /// </summary>
        /// <param name="continuationFactory">Delegate that creates the managed task to run after the current one successfully executes. Null can be returned if no task needs to be scheduled</param>
        /// <returns>Current options for method chaining</returns>
        IManagedAnonymousTaskCreationOptions<TOutput> OnSuccess(Func<ITaskManager, IManagedAnonymousTask, object, CancellationToken, Task<IManagedTask>?> continuationFactory)
        {
            continuationFactory.ValidateArgument(nameof(continuationFactory));

            return ContinueWith((m, p, o, t) =>
            {
                if (o is TOutput output) return continuationFactory(m, p, output, t);
                return null;
            });
        }
        /// <summary>
        /// Defines the factory for another managed task to run after the current one fails to execute.
        /// </summary>
        /// <param name="continuationFactory">Delegate that creates the managed task to run after the current one fails to execute. Null can be returned if no task needs to be scheduled</param>
        /// <returns>Current options for method chaining</returns>
        IManagedAnonymousTaskCreationOptions<TOutput> OnFail(Func<ITaskManager, IManagedAnonymousTask, object, CancellationToken, Task<IManagedTask>?> continuationFactory)
        {
            continuationFactory.ValidateArgument(nameof(continuationFactory));

            return ContinueWith((m, p, o, t) =>
            {
                if (o is Exception exception) return continuationFactory(m, p, exception, t);
                return null;
            });
        }

        /// <summary>
        /// Defines the factory for another managed task to run after the current one successfully executes.
        /// </summary>
        /// <param name="continuationFactory">Delegate that creates the managed task to run after the current one successfully executes. Null can be returned if no task needs to be scheduled</param>
        /// <returns>Current options for method chaining</returns>
        IManagedAnonymousTaskCreationOptions<TOutput> OnSuccess(Func<ITaskManager, IManagedAnonymousTask, TOutput, CancellationToken, IManagedTask?> continuationFactory)
        {
            continuationFactory.ValidateArgument(nameof(continuationFactory));

            return ContinueWith((m, p, o, t) =>
            {
                if (o is TOutput output) return continuationFactory(m, p, output, t);
                return null;
            });
        }
        /// <summary>
        /// Defines the factory for another managed task to run after the current one fails to execute.
        /// </summary>
        /// <param name="continuationFactory">Delegate that creates the managed task to run after the current one fails to execute. Null can be returned if no task needs to be scheduled</param>
        /// <returns>Current options for method chaining</returns>
        IManagedAnonymousTaskCreationOptions<TOutput> OnFail(Func<ITaskManager, IManagedAnonymousTask, Exception, CancellationToken, IManagedTask?> continuationFactory)
        {
            continuationFactory.ValidateArgument(nameof(continuationFactory));

            return ContinueWith((m, p, o, t) =>
            {
                if (o is Exception exception) return continuationFactory(m, p, exception, t);
                return null;
            });
        }

        /// <summary>
        /// Defines the factory for another managed task to run after the current one successfully executes.
        /// </summary>
        /// <param name="continuationFactory">Delegate that creates the managed task to run after the current one successfully executes. Null can be returned if no task needs to be scheduled</param>
        /// <returns>Current options for method chaining</returns>
        IManagedAnonymousTaskCreationOptions<TOutput> OnSuccess(Func<ITaskManager, IManagedAnonymousTask, TOutput, CancellationToken, IManagedAnonymousTask?> continuationFactory)
        {
            continuationFactory.ValidateArgument(nameof(continuationFactory));

            return ContinueWith((m, p, o, t) =>
            {
                if (o is TOutput output) return continuationFactory(m, p, output, t);
                return null;
            });
        }
        /// <summary>
        /// Defines the factory for another managed task to run after the current one fails to execute.
        /// </summary>
        /// <param name="continuationFactory">Delegate that creates the managed task to run after the current one fails to execute. Null can be returned if no task needs to be scheduled</param>
        /// <returns>Current options for method chaining</returns>
        IManagedAnonymousTaskCreationOptions<TOutput> OnFail(Func<ITaskManager, IManagedAnonymousTask, Exception, CancellationToken, IManagedAnonymousTask?> continuationFactory)
        {
            continuationFactory.ValidateArgument(nameof(continuationFactory));

            return ContinueWith((m, p, o, t) =>
            {
                if (o is Exception exception) return continuationFactory(m, p, exception, t);
                return null;
            });
        }
        #endregion
    }
}
